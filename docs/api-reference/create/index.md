---
title: 'Create a function'
sidebar_position: 0
description: 'The definers every wiring eventually points at, and the config and services they run against.'
---

# Create a function

The definers every wiring eventually points at, and the config and services they run against.

| Door | Exports | What it is for |
| --- | --- | --- |
| [`#pikku/function`](./function.md) | 20 | The function definers every wiring eventually points at, and the types they are written against. A function is handed services, then its input, then the wire —… |
| [`#pikku/setup`](./setup.md) | 4 | The three factories a project declares exactly once — its config, its singleton services and its per-wire services. An addon declares the same three in its own… |
